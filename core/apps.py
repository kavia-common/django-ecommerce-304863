"""Django app configuration for the `core` application."""

from django.apps import AppConfig


class CoreConfig(AppConfig):
    """Application configuration for the core e-commerce domain app."""

    name = "core"
