# Python 3.12 compatible requirements (pinned).
#
# Chosen Django version: 4.2.27 (LTS)
# Rationale: Django 4.2 is the long-term support branch and tends to require fewer
# breaking changes than Django 5.x/6.x while still supporting Python 3.12.
#
# NOTE: Legacy django-allauth/django-countries versions used django.utils.six
# (or six-style importers) and are not compatible with modern Python/Django.
# These pins intentionally move to versions that no longer rely on django.utils.six.

# Core framework
Django==4.2.27

# API stack
djangorestframework==3.15.2
djangorestframework-simplejwt==5.4.0

# Auth
django-allauth==0.63.6

# Forms / UI
django-crispy-forms==2.1
crispy-bootstrap4==2024.10

# Countries
django-countries==7.6.1

# Dev tools
django-debug-toolbar==4.4.6

# Security headers / CSP
django-csp==3.8

# Config
python-decouple==3.8

# Stripe
stripe==10.12.0

# Imaging
Pillow==10.4.0

# HTTP/OAuth helpers (allauth dependencies / common)
requests==2.32.3
requests-oauthlib==2.0.0
oauthlib==3.3.1
python3-openid==3.2.0
defusedxml==0.7.1

# Django/runtime utilities
sqlparse==0.5.1

# Formatting (kept, but modernized to avoid old pep8 tooling issues on new Python)
autopep8==2.0.4
pycodestyle==2.11.1
