# Runtime dependencies (production) for the combined Django + legacy Flask runtime.
#
# Install with:
#   pip install -r requirements.txt
#
# Development tooling (linters/formatters/test utilities/debug toolbar) lives in requirements-dev.txt.

# Python runtime (observed): 3.12.x
Django==5.2.10
django-allauth==65.13.1
django-countries==8.2.0
django-crispy-forms==2.5
crispy-bootstrap4==2025.6

# Payments
stripe==14.1.0

# Configuration / assets
python-decouple==3.8
Pillow==12.1.0

# Utilities commonly required by allauth/requests integrations
defusedxml==0.7.1
oauthlib==3.2.2
requests==2.32.3
requests-oauthlib==2.0.0
python3-openid==3.2.0
certifi==2025.1.31
idna==3.10
urllib3==2.3.0

# WSGI server for unified single-port deployment (Django + Flask mounted at /flask)
gunicorn==23.0.0

############################
# Flasky (mounted at /flask in the unified combined_wsgi runtime)
#
# This Flasky codebase is legacy and expects Flask 2.x + Flask-SQLAlchemy 2.x + SQLAlchemy 1.x.
# Keep these constraints to avoid breaking the legacy runtime.
############################
Flask<3
Flask-SQLAlchemy<3
SQLAlchemy<2
flask-bootstrap
Flask-Login
Flask-Mail
Flask-Moment
Flask-PageDown
Flask-WTF
Flask-HTTPAuth
bleach
